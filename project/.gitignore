# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*.[oa]
*.so

# C extensions
*.pyd
*.dll

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover

# Django stuff
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
/media/
/staticfiles/

# Environments
.env
.venv
env/
venv/

# IDE settings
.idea/
.vscode/

# --- ML Model Files (ml_models/) ---
# Ignore everything in the ml_models directory by default.
/ml_models/*

# Now, re-include (don't ignore) the essential files we want to track.
!/ml_models/train_model_new.ipynb
!/ml_models/ridge_model.pkl
!/ml_models/tfidf_vectorizers.pkl
!/ml_models/lda_model.pkl
!/ml_models/lda_count_vectorizer.pkl
!/ml_models/model_results.pkl
!/ml_models/numerical_features_summary.csv

# We need to un-ignore the directory itself so Git can look inside it.
!/ml_models/
/json_data/*